import React from "react";

import { SectorCallout } from "./index";

export default {
  component: SectorCallout,
  title: "Components/SectorCallout",
};

export const Replay = (args) => <SectorCallout {...args} />;
